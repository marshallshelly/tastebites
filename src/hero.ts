import { heroui } from "@heroui/react";
export default heroui({
  themes: {
    light: {
      colors: {
        default: {
          DEFAULT: "#000",
          foreground: "#fff",
        },
      },
    },
  },
});
